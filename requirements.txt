# Core dependencies for RCS simulations with openEMS
numpy>=1.20.0
matplotlib>=3.3.0
cython>=0.29.0
h5py>=3.0.0

# Note: openEMS and CSXCAD need to be installed separately
# See setup_scripts_and_notes/install_openems.sh for installation instructions
